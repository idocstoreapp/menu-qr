---
import PublicLayout from '../layouts/PublicLayout.astro';
import HalalInfo from '../components/HalalInfo';
import NavigationMenu from '../components/NavigationMenu';
import { supabase } from '../lib/supabase';

// Verificar configuración de Supabase
const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL || import.meta.env.SUPABASE_URL;
const supabaseKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY || import.meta.env.SUPABASE_ANON_KEY;
const isConfigured = !!(supabaseUrl && supabaseKey);

// Obtener categorías activas que tienen items disponibles
let activeCategories: any[] = [];
let featuredItems: any[] = [];

if (isConfigured && supabaseUrl !== 'https://placeholder.supabase.co') {
  try {
    const { data: allItems, error: itemsError } = await supabase
      .from('menu_items')
      .select('category_id')
      .eq('is_available', true);

    if (itemsError) {
      console.error('Error obteniendo items:', itemsError);
    } else if (allItems) {
      const categoryIdsWithItems = [...new Set((allItems || []).map(item => item.category_id).filter(Boolean))];

      if (categoryIdsWithItems.length > 0) {
        const { data: cats, error: catsError } = await supabase
          .from('categories')
          .select('*')
          .eq('is_active', true)
          .in('id', categoryIdsWithItems)
          .order('order_num', { ascending: true });

        if (!catsError && cats) {
          activeCategories = cats || [];
        }
      }

      // Obtener items destacados
      const { data: featured, error: featuredError } = await supabase
        .from('menu_items')
        .select('*, category:categories(name, slug)')
        .eq('is_available', true)
        .eq('is_featured', true)
        .limit(4);

      if (!featuredError && featured) {
        featuredItems = featured || [];
      }
    }
  } catch (error: any) {
    // No fallar el build si hay error, solo loguear
    console.error('Error conectando con Supabase:', error?.message || error);
  }
}
---

<PublicLayout title="Menú Digital - Gourmet Árabe">
  <!-- Menú de navegación sticky -->
  <NavigationMenu client:load categories={activeCategories} />
  
  <!-- Fondo con patrón árabe -->
  <div class="min-h-screen bg-[#0a0a0a] relative">
    <!-- Patrón árabe de fondo -->
    <div class="absolute inset-0 opacity-10" style="background-image: url('/fondo.png'); background-size: cover; background-position: center;"></div>
    
    <div class="relative z-10 flex flex-col min-h-screen">
      <!-- Header con Logo Grande -->
      <header class="flex-grow flex flex-col items-center justify-center py-12 px-4">
        <div class="text-center animate-fade-in">
          <!-- Logo -->
          <div class="relative mx-auto w-56 h-56 md:w-80 md:h-80 lg:w-96 lg:h-96 mb-6">
            <div class="absolute inset-0 bg-gold-500/20 rounded-full blur-3xl animate-pulse"></div>
            <img 
              src="/logo-cropped.png" 
              alt="Gourmet Árabe" 
              class="relative w-full h-full object-contain drop-shadow-[0_0_30px_rgba(212,175,55,0.5)] animate-float"
            />
          </div>
          
          <!-- Título -->
          <h1 class="font-cinzel text-gold-400 text-3xl md:text-4xl tracking-[0.3em] mb-2" style="text-shadow: 0 0 30px rgba(212,175,55,0.4);">
            GOURMET ÁRABE
          </h1>
          <p class="text-gold-300/70 font-playfair text-lg italic mb-4">
            Sabores Auténticos del Medio Oriente
          </p>

          <!-- Información Halal -->
          <div class="mb-8 max-w-2xl mx-auto px-4">
            <div class="border border-gold-600/40 rounded-lg p-4 bg-black/50 backdrop-blur-sm">
              <div class="flex items-center justify-center gap-2 mb-2">
                <span class="text-2xl">🕌</span>
                <p class="text-gold-400 font-cinzel text-sm font-semibold">
                  CARNE HALAL CERTIFICADA
                </p>
              </div>
              <p class="text-gold-300/80 text-xs text-center leading-relaxed">
                Trabajamos exclusivamente con carne halal: animales criados y sacrificados según las leyes islámicas, 
                con trato ético, dieta vegetariana sin hormonas ni antibióticos, y método de sacrificio ritual respetuoso.
              </p>
            </div>
          </div>

          {activeCategories.length > 0 ? (
            <div class="space-y-8">
              <!-- Mensaje -->
              <div class="inline-block">
                <div class="border-2 border-gold-600/50 rounded-lg px-6 py-3 bg-black/60 backdrop-blur-sm">
                  <p class="text-gold-300 text-lg">Selecciona una categoría del menú</p>
                </div>
              </div>

              <!-- Grid de categorías -->
              <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 max-w-4xl mx-auto">
                {activeCategories.map((category, index) => (
                  <a
                    href={`/${category.slug}`}
                    class="category-btn group relative overflow-hidden"
                    style={`animation-delay: ${index * 0.1}s`}
                  >
                    <div class="
                      relative border-2 border-gold-500 rounded-lg p-4 
                      bg-gradient-to-b from-gold-600/10 to-transparent
                      transition-all duration-300
                      group-hover:border-gold-400 
                      group-hover:bg-gradient-to-b group-hover:from-gold-500 group-hover:to-gold-600
                      group-hover:shadow-[0_0_25px_rgba(212,175,55,0.5)]
                    ">
                      <!-- Decoración de esquinas -->
                      <div class="absolute top-0 left-0 w-3 h-3 border-t-2 border-l-2 border-gold-400 rounded-tl opacity-50 group-hover:opacity-100"></div>
                      <div class="absolute top-0 right-0 w-3 h-3 border-t-2 border-r-2 border-gold-400 rounded-tr opacity-50 group-hover:opacity-100"></div>
                      <div class="absolute bottom-0 left-0 w-3 h-3 border-b-2 border-l-2 border-gold-400 rounded-bl opacity-50 group-hover:opacity-100"></div>
                      <div class="absolute bottom-0 right-0 w-3 h-3 border-b-2 border-r-2 border-gold-400 rounded-br opacity-50 group-hover:opacity-100"></div>
                      
                      <span class="
                        relative font-cinzel text-gold-400 font-bold text-sm md:text-base tracking-wide
                        group-hover:text-black transition-colors duration-300
                      ">
                        {category.name.toUpperCase()}
                      </span>
                    </div>
                  </a>
                ))}
              </div>

              <!-- Items destacados -->
              {featuredItems && featuredItems.length > 0 && (
                <div class="mt-12 max-w-4xl mx-auto">
                  <div class="border-2 border-gold-600/30 rounded-lg p-6 bg-black/40 backdrop-blur-sm">
                    <h3 class="font-cinzel text-gold-400 text-xl mb-4 text-center">
                      ⭐ DESTACADOS ⭐
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                      {featuredItems.map((item: any) => (
                        <a href={`/${item.category?.slug || ''}`} class="block group">
                          <div class="flex items-center gap-4 p-3 rounded-lg border border-gold-600/30 hover:border-gold-500 hover:bg-gold-600/10 transition-all">
                            {item.image_url && (
                              <img 
                                src={item.image_url} 
                                alt={item.name}
                                class="w-16 h-16 object-cover rounded-lg border border-gold-600/50"
                              />
                            )}
                            <div class="flex-1 min-w-0">
                              <h4 class="text-gold-400 font-semibold truncate">{item.name}</h4>
                              <p class="text-gold-300/60 text-sm truncate">{item.description}</p>
                            </div>
                            <span class="text-gold-400 font-bold whitespace-nowrap">
                              ${item.price.toLocaleString('es-CL')}
                            </span>
                          </div>
                        </a>
                      ))}
                    </div>
                  </div>
                </div>
              )}
            </div>
          ) : (
            <div class="border-2 border-gold-600/50 rounded-lg p-8 bg-black/60 backdrop-blur-sm max-w-md mx-auto">
              {!isConfigured ? (
                <>
                  <p class="text-red-400 text-xl mb-4">⚠️ Configuración Pendiente</p>
                  <p class="text-gold-300/70 mb-4">
                    Las variables de entorno de Supabase no están configuradas en producción.
                  </p>
                  <div class="bg-red-900/30 border border-red-600/50 rounded p-4 mb-4 text-sm">
                    <p class="text-red-300 mb-2 font-semibold">Configura estas variables:</p>
                    <ul class="text-red-200/80 space-y-1 list-disc list-inside">
                      <li>PUBLIC_SUPABASE_URL</li>
                      <li>PUBLIC_SUPABASE_ANON_KEY</li>
                      <li>JWT_SECRET</li>
                    </ul>
                  </div>
                  <p class="text-gold-300/70 text-xs mb-4">
                    Revisa el archivo <code class="bg-black/50 px-2 py-1 rounded">CONFIGURACION_PRODUCCION.md</code> para más detalles.
                  </p>
                  <a 
                    href="/api/check-config" 
                    target="_blank"
                    class="inline-block px-6 py-2 border-2 border-gold-600 text-gold-400 rounded-lg hover:bg-gold-600 hover:text-black transition-all text-sm"
                  >
                    Verificar Configuración
                  </a>
                </>
              ) : (
                <>
                  <p class="text-gold-400 text-xl mb-4">Menú en preparación</p>
                  <p class="text-gold-300/70 mb-6">Estamos preparando nuestro menú digital</p>
                  <a 
                    href="/admin/login" 
                    class="inline-block px-6 py-2 border-2 border-gold-600 text-gold-400 rounded-lg hover:bg-gold-600 hover:text-black transition-all"
                  >
                    Acceso Admin
                  </a>
                </>
              )}
            </div>
          )}
        </div>
      </header>

      <!-- Footer -->
      <footer class="border-t-2 border-gold-600/30 bg-black/80 py-6">
        <div class="max-w-4xl mx-auto px-4 text-center">
          <p class="text-gold-400 font-playfair mb-2">
            Gourmet Árabe - Sabores Auténticos del Medio Oriente
          </p>
          <p class="text-gold-300/70 text-sm mb-4">
            📱 +56 9 3945 9286
          </p>
          <div class="flex justify-center gap-6">
            <a href="/print" class="text-gold-400/70 hover:text-gold-400 text-sm transition">🖨️ Menú Imprimible</a>
            <a href="/admin/login" class="text-gold-400/70 hover:text-gold-400 text-sm transition">🔐 Admin</a>
          </div>
        </div>
      </footer>
    </div>
  </div>

  <!-- Componente flotante de información Halal -->
  <HalalInfo client:load />
</PublicLayout>

<style>
  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
  }
  
  .category-btn {
    animation: fadeInUp 0.5s ease-out forwards;
    opacity: 0;
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
